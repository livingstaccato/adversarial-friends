[build-system]
requires = ["setuptools>=77", "wheel>=0.43"]
build-backend = "setuptools.build_meta"

[project]
name = "afriends"
version = "0.6.0"
description = "Reservation compatibility package for afriend"
requires-python = ">=3.11"
license = "MIT"
dependencies = ["afriend==0.6.0"]

[tool.setuptools]
packages = []
